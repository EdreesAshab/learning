import { NameInitialsCircleDirective } from './name-initials-circle.directive';

describe('NameInitialsCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new NameInitialsCircleDirective();
    expect(directive).toBeTruthy();
  });
});
