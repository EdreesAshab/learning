import { InitialsCircleDirective } from './initials-circle.directive';

describe('InitialsCircleDirective', () => {
  it('should create an instance', () => {
    const directive = new InitialsCircleDirective();
    expect(directive).toBeTruthy();
  });
});
